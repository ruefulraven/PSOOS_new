## PSOOS System

# to enable the image via server.
# install http-server first,
# download npm if npm is not downloaded yet.
# after download, run following command in cmd --> npm install -g http-server
# after download complete, go to mug and shirt folder, run those following commands --> http-server
# adjust the http server ports in the code accordingly